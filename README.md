# my-auth-app

โปรเจกต์ Node.js สำหรับระบบ Authentication พื้นฐาน

## วิธีรัน
1. `npm install`
2. สร้างไฟล์ `.env` ใส่ `JWT_SECRET` กับ `PORT=3000`
3. `node index.js`

## หมายเหตุ
ไฟล์ `.env` ถูกกันด้วย `.gitignore` เพื่อความปลอดภัย
